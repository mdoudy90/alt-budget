import React from "react";
//will take in wishlist data;
//render list of items linked from wishlist;

const Wishlist = (props) => (
  <div className='wishlist-list'>
    <h1>My Wishlist</h1>
    <table>
      <tr>
        <td>wishlist item</td>
      </tr>
      </table>
  </div>
);

export default Wishlist;
